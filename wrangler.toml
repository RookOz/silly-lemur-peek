name = "framecontrol"
pages_build_output_dir = "dist"
compatibility_date = "2024-04-03"

# Cloudflare Pages does not use the [site] or [env] blocks in the same way as Workers.
# The build output directory is defined above.